class Company(object):
    def __init__(self, name):
        self.name = name
    import datetime
    
    # name = "Unknown"
    address = "123 Example Ave."
    phone = 'unknown'
    email = 'example@example.com'
    category = 'unknown'
    subcategory = 'unknown'
    site = 'http://www.example.com'
    createddate = datetime.date.today()
    #remittances

#Create set of Companies    
#for i in range(4):
#	y = set()
#	x = Company("Company %s" % str(i + 1))
#	y.add(x)
#
